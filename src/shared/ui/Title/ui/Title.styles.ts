import { styled } from '@/app/stitches.config';

export const Title = styled('h1', {
  position: 'relative',
  fontSize: '1.75rem',
  fontWeight: 600,
  lineHeight: '5rem',
  color: '#fff',
  textAlign: 'center',
});