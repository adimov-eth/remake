import { styled } from '@app/stitches.config';

export const Button = styled('button', {
  position: 'relative',
  display: 'block',
  width: '100%',
  height: '100%',
  backgroundColor: 'transparent',
  border: 'none',
  cursor: 'pointer',
});
