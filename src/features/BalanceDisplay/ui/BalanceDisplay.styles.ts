import { styled } from '@stitches/react';

export const Root = styled('div', {
  display: 'inline-flex',
  alignItems: 'center',
  gap: '0.375rem',
});

export const Icon = styled('svg', {
  width: '1.5rem',
  height: '1.5rem',
});